# Include outdated status in the first release

Bare `plugman outdated` checks every installed official plugin and every GitHub plugin with a Source Record, reporting current and newest compatible versions, enabled state, and source without changing the vault. This supplies the useful decision step before `update` without introducing AI-generated summaries.
