# Include local plugin listing

The first release includes `plugman list` for all installed Community Plugins, `--enabled` filtering, and stable `--json` output. It reports version, enabled state, and known source using only vault-local information and performs no network requests.
