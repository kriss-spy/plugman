# Support Linux, macOS, and Windows desktop

The first public release supports Linux, macOS, and Windows desktop and does not provide an Android or iOS CLI. Platform-specific live integration must fail safely if unavailable, and desktop-only plugin metadata is reported rather than treated as an installation error because Plugman itself operates on desktop vaults.
