# Build Plugman as a standalone CLI first

Plugman will initially be a standalone command-line product rather than an Obsidian community plugin. This keeps package management usable for headless setup, scripting, and recovery when Obsidian is closed or unhealthy; a community-plugin interface may be added later as a client of the same product behavior.
