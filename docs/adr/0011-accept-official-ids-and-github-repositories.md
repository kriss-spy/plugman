# Accept official plugin IDs and GitHub repositories

Plugman accepts direct official community-directory plugin IDs, Plugin List paths, and explicit public GitHub repository URLs as Plugin Inputs, including mixed inputs in one command. This preserves convenient one-off installation as well as reusable lists while making manual installation of GitHub-hosted Obsidian plugins possible; other hosts, private repositories, and local source trees remain outside the initial scope.
