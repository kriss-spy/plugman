# Use simple line-oriented Plugin Lists

A Plugin List contains one Plugin Declaration per line; official IDs accept an optional `@exact-version`, while GitHub inputs use repository or exact release URLs. Blank lines and `#` comments are allowed, filenames are unrestricted because paths are explicit, and version ranges are deferred to keep authoring and conflict behavior understandable.
